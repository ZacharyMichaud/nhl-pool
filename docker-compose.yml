services:
  database:
    image: postgres:14-alpine
    environment:
      POSTGRES_DB: nhlpool
      POSTGRES_USER: nhlpool
      POSTGRES_PASSWORD: nhlpool
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
